<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme chính -->
    <style name="Theme.Smarthomevoice" parent="Theme.MaterialComponents.Light.NoActionBar">

    </style>

    <style name="Widget.MyCustomSwitch"
        parent="@style/Widget.MaterialComponents.CompoundButton.Switch">

        <!-- Bỏ hiển thị chữ ON/OFF -->
        <item name="android:showText">false</item>

        <!-- Override track & thumb bằng file drawable selector -->
        <item name="android:track">@drawable/switch_track_selector</item>
        <item name="android:thumb">@drawable/switch_thumb_selector</item>
    </style>
    <style name="HintTextStyle" parent="TextAppearance.AppCompat">
        <item name="android:textColor">#7F8C8D</item>
    </style>

</resources>
